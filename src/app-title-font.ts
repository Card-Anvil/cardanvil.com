/** Loaded via `index.html` (Google Fonts → Outfit). */
export const APP_TITLE_FONT_FAMILY = "'Outfit', sans-serif";

/** Use for every visible “Card Anvil” title (nav + hero, etc.). */
export const APP_TITLE_FONT_WEIGHT = "semibold" as const;
